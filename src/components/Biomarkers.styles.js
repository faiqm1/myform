import { makeStyles } from '@mui/styles'

export const Biomarkersstyles = makeStyles({
  flexContainer: {
    display: 'flex',
    justifyContent: 'space-between',
  },
  tableheadingspace: {
    marginTop: 3,
  },
})
